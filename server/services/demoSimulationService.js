// Orchestrates the demo simulation.
// TODO(Gia): implement lock -> final score -> calculate points (via scoringService)
// -> update leaderboard, called from controllers/demoController.

module.exports = {};
